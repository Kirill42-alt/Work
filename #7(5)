n = int(input())
a = int(input())
b = int(input())
if b != 0 and n > 0 and n < 5:
    act = {
        1: a + b,
        2: a - b,
        3: a * b,
        4: a / b
    }
    
    print(act[n])
    
else:
    print("Server error")
